(in-package :cl-user)

(defpackage :cdsi
  (:use :cl
        :cdsi.common
        :cdsi.calendar
        :cdsi.health-records
        :cdsi.supporting-data
        :cdsi.gather-info))

(in-package :cdsi)

